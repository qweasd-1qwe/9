<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.walletbill">

    <application android:allowBackup="true" android:label="WalletBill">

        <!-- NotificationListenerService: 用于监听系统通知（请确保用户授予通知访问权限） -->
        <service android:name=".NotificationListenerServiceImpl"
                 android:label="WalletBillNotificationListener"
                 android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>



